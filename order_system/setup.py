#!/usr/bin/python3

from setuptools import setup, find_packages
import pathlib

setup(
        name='order_system',
        version='0.0.1',
        packages=find_packages(where='hanlder'),
        install_requires=[]
        )
